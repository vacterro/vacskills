Test: `BOARD.md` has no tickets in `## TODO` and the user runs bare `saipen` -- `## DONE` or `## BLOCKED` MAY still carry tickets from earlier work; per `phases/done.md`'s explicit step 2, their presence does NOT block this transition. Per RFC § 2.1 ZERO-PROMPT AUTO-TRANSITION, the agent MUST NOT ask "should I hunt or add?" -- it silently transitions to `HUNT`, and to `ADD` immediately if `HUNT` comes back clean. This is a behavioral test (agent decision-making), not a structural one -- no `.saipen/` fixture to validate against; the assertion is entirely in the agent's response to the empty-TODO condition, not a literally empty board.
