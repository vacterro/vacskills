Test: `BOARD.md` has a `[/]` ticket under `## DOING` with NO `owner:`/`claim_time:` fields (a crashed single-agent session, or pre-claim history). Per RFC § 1.4, such a ticket is unclaimed by definition -- a cold agent MUST NOT treat it as untouchable or ask the user what to do with it. Correct behavior: LOG a `DEC` line recording the adoption, inspect `.saipen/kitchen/` for the predecessor's half-done work before redoing anything, then continue the ticket. Behavioral test (agent decision-making) -- the assertion is the adoption procedure, not file shape, so there is no `.saipen/` fixture to validate mechanically.
