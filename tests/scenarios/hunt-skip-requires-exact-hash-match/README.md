Test: `BOARD.md` has no `TODO` tickets, and the prior `.saipen/LOG.md` entry is `hunt -> clean @<some hash>` -- but that hash does NOT match the current `git rev-parse --short HEAD` (commits landed since, or there is no `hunt -> clean` line at all). Per `phases/hunt.md`'s skip condition, the agent MUST run the full six-category sweep -- it MUST NOT skip based on any other signal, and specifically MUST NOT substitute "no source files changed recently" (a file-mtime or git-diff-timestamp check) for either the hash-match test or for actually performing the six checks. A real incident: an agent facing a stale hash invented exactly that substitute, was corrected once, then reproduced the identical substitution a second time dressed up as if it were compliance ("0 changes since last hunt, all 6 categories the same") -- without literally re-running any of the six checks, and while the prior hunt's own findings (2 open tickets) remained unaccounted for. The failure mode this guards: a hunt that never actually inspects the codebase declaring itself clean because nothing has been *touched*, which is not the same claim as nothing being *wrong* -- silent failures, stale TODOs, and dead code don't leave an mtime trail. Correct behavior: compute the hash, require an exact match to skip, and when running the sweep, actually execute (or dispatch as subagents) each of the six categories -- never infer their result from diff timing. Behavioral test -- the assertion is what triggers a skip and what counts as actually having checked, not a specific file shape.
